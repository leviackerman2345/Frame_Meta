import { AuthForm } from "@/components/auth/AuthForm";

export const metadata = {
  title: "Sign In — FrameMeta",
  description: "Sign in to your FrameMeta account to discover cinema.",
};

export default function LoginPage() {
  return <AuthForm />;
}
